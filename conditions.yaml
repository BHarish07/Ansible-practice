- name: conditions
  hosts: web
  tasks:
   - name: check user exist or not 
     ansible.builtin.command: id expense
     register: user
     ignore_errors: true

   - name: user info 
     ansible.builtin.debug:
      msg: " User info : {{user}}"
     
   - name: Hello
     ansible.builtin.debug:
      msg: "Hello..." 